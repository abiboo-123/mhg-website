---
const { name, logo, description, website } = Astro.props;
---

<div class="bg-white shadow-md rounded-xl p-6 flex flex-col items-center text-center space-y-4">
  <img src={logo} alt={name + ' logo'} class="w-24 h-24 object-contain" />
  <h3 class="text-lg font-semibold">{name}</h3>
  <p class="text-sm text-slate-600">{description}</p>
  <a href={website} target="_blank" class="text-sm text-sky-600 underline hover:text-sky-800">Visit Website</a>
</div>
